# 一期切片1—4原型对齐与客户演示收口验收证据

## 基线与范围

- Base：`8fe913414572918aabadac6b528b123857e34143`
- Branch：`feature/phase1-slice1-4-frontend-prototype-alignment-demo-closeout`
- Backend / Migration / SQL / package-lock：前端人为差异 0
- 未实施：Slice 5、真实部署、依赖漏洞治理、后端与 API 合同修改

## 静态质量门禁

| 门禁 | 结果 |
|---|---|
| 锁文件复现安装 | PASS |
| Vitest | 129/129 PASS，skip 0 |
| TypeScript + Production Build | PASS |
| Lint | PASS |
| Slice 4 Lint | PASS |
| CRLF-safe 变更差异审计 | PASS |
| git diff --check | PASS |
| PII/PHI/Credential scan | 0 命中 |

仓库级 `format:check` 仍会报告既有文件的缩进与 CRLF 基线；本轮遵守“不批量格式化历史文件”，没有为产品语义纠偏引入整文件机械重排。所有本轮源代码差异均通过 Lint 与 `git diff --check`，该历史格式例外不伪报为 GREEN，并交由远端 CI 在标准 LF checkout 下复核。

## 真实 HTTP

环境：Fresh Disposable PostgreSQL、正式 Migration、正式 FastAPI、随机一次性密钥与账号、仅 loopback。

| 合同 | 结果 |
|---|---|
| 平台待审核队列 | 200 |
| 平台用户实名队列 | 200 |
| 机构健管师团队 | 200 |
| 平台组织树 | 200 |
| Slice 4 健康档案 | 200 |
| Slice 4 检测报告元数据 | 200 |
| Slice 4 最新指标 | 200 |
| Slice 4 评估准备 | 200 |
| 缺少 Authorization | 401 `AUTHENTICATION_REQUIRED` |
| Bearer 格式错误 | 401 `AUTHENTICATION_REQUIRED` |
| 无效或过期 Token | 401 `AUTHENTICATION_REQUIRED` |
| 平台角色访问机构健康档案 | 403 `INSTITUTION_SCOPE_FORBIDDEN` |
| 跨租户访问 | 403 `INSTITUTION_SCOPE_FORBIDDEN` |
| 不存在服务案例 | 404 `SERVICE_CASE_NOT_FOUND` |
| 非法参数 | 422 `INVALID_REQUEST` |
| Disposable 数据库停止 | 503 `DEPENDENCY_UNAVAILABLE` |

前端未把 401/403/404/422/503 包装为成功；HTTP 验收输出敏感值 0。

## 浏览器与布局

- 1440×900：22/22 正向页面完成。
- 1366×768：6/6 代表页面完成。
- 1024×768：6/6 代表页面完成。
- 横向溢出：0。
- 浏览器 Console error：0。
- 1440 布局矩形：侧栏 256px；右侧工作区 1168.8px；内容容器扣除 24px 双侧安全边距后 1120.8px。
- 键盘焦点：统一 `focus-visible` 双像素品牌蓝轮廓。
- reduced-motion：保留系统偏好降级规则。

## 产品语义专项

- 正式导航不再出现“会员邀请”“会员入组”或正式“健管师邀请”。
- 服务关系统一展示为“客户服务邀约”“服务客户”“客户服务接入”，未包装为会员购买或付费权益。
- 平台身份流程统一展示为“用户实名认证审核”。
- 健管师正式名单展示为“健管师团队”；一期邀请路由保留为“试点健管师接入”，并明确不代表正式开放注册流程。
- 路由、API path、DTO、权限、错误合同及后端枚举未变。

## 安全边界

- 登录页、输入框值、JWT、Cookie、Storage 和请求体未进入截图或证据。
- 完整身份信息默认隐藏；截图只出现脱敏证件。
- 健康档案只展示获批摘要、最新指标和报告元数据，不展示病史、过敏、用药、症状或报告原件。
- 页面明确显示“输入条件齐备不等于已产生评估结果”。

## Disposable 清理

- PostgreSQL 容器：0
- 8000 / 5174 监听端口：0
- FastAPI / Vite / 验收控制器进程：0
- 一次性凭据文件：0
- 本轮临时验收目录：0
- 浏览器验收标签页：已关闭；临时视口：已重置
- Docker 容器、Network、Volume：0
