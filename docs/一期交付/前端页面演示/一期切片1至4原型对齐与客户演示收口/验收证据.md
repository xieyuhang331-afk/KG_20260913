# 一期切片1—4原型对齐与客户演示收口验收证据

## 基线与范围

- 原 PR #63 Base：`8fe913414572918aabadac6b528b123857e34143`
- 最终 Rebaseline develop：`c9f298f4791efd2c1e2e24711b2799cfca5d0829`（包含后端 Hotfix PR #64 与已合并 PR #65）
- PR #65 增量：73 个 workflow/backend/Migration/后端测试文件，frontend 文件 0；未发生前端冲突或合同漂移。
- 两次普通 Merge 后本地 HEAD：`9de5f0e4c35d68d83217bbbf583a9e42324915c8`
- Branch：`feature/phase1-slice1-4-frontend-prototype-alignment-demo-closeout`
- Backend / Migration / SQL / package-lock：前端人为差异 0
- 未实施：Slice 5、真实部署、依赖漏洞治理、后端与 API 合同修改

## 静态质量门禁

| 门禁 | 结果 |
|---|---|
| 锁文件复现安装 | PASS |
| Vitest | 138/138 PASS，skip 0 |
| TypeScript + Production Build | PASS |
| Lint | PASS |
| Slice 4 Lint | PASS |
| CRLF-safe 变更差异审计 | PASS |
| git diff --check | PASS |
| PII/PHI/Credential scan | 0 命中 |

仓库级 `format:check` 仍会报告既有文件的缩进与 CRLF 基线；本轮遵守“不批量格式化历史文件”，没有为产品语义纠偏引入整文件机械重排。所有本轮源代码差异均通过 Lint 与 `git diff --check`，该历史格式例外不伪报为 GREEN，并交由远端 CI 在标准 LF checkout 下复核。

## 真实 HTTP

环境：Fresh Disposable PostgreSQL、正式 Migration、正式 FastAPI、随机一次性密钥与账号、仅 loopback。

正式 Migration Head：`20260825_0030`。

| 合同 | 结果 |
|---|---|
| 平台待审核队列 | 200 |
| 平台用户实名队列 | 200 |
| 机构健管师团队 | 200 |
| 平台组织树 | 200 |
| Slice 4 健康档案 | 200 |
| Slice 4 检测报告元数据 | 200 |
| Slice 4 最新指标 | 200 |
| Slice 4 评估准备 | 200 |
| 缺少 Authorization | 401 `AUTHENTICATION_REQUIRED` |
| Bearer 格式错误 | 401 `AUTHENTICATION_REQUIRED` |
| 无效或过期 Token | 401 `AUTHENTICATION_REQUIRED` |
| 平台角色访问机构健康档案 | 403 `INSTITUTION_SCOPE_FORBIDDEN` |
| 跨租户访问 | 403 `INSTITUTION_SCOPE_FORBIDDEN` |
| 不存在服务案例 | 404 `SERVICE_CASE_NOT_FOUND` |
| 非法参数 | 422 `INVALID_REQUEST` |
| Disposable 数据库停止 | 503 `DEPENDENCY_UNAVAILABLE` |

### 客户服务接入正式旅程

- 创建服务客户邀约并由用户接受；实名材料经正式审核达到 `VERIFIED`。
- 必要服务同意记录通过正式业务旅程建立。
- 主健管师分配初始为 `PENDING_ACCEPTANCE`，健管师接受接口返回 201。
- 同一 Idempotency-Key 重放返回相同结果，没有重复分配或重复 ServiceCase。
- 重新查询分配返回 200 且状态为 `ACCEPTED`；ServiceCase 由正式流程形成并处于 `PREPARING`。
- 对应真实数据库闭环测试 JUnit：1/0/0/0（tests/failures/errors/skipped）。
- 在最终 PR #65 / Migration 0030 基线上重新执行同一真实旅程：JUnit 1/0/0/0，敏感信息命中 0，Disposable 残留 0。
- 页面截图使用该合法旅程的 enrollment、assignment 与 ServiceCase；未直接写表制造最终展示状态。

前端未把 401/403/404/422/503 包装为成功；HTTP 验收输出敏感值 0。

## 浏览器与布局

- 1440×900：22/22 正向页面完成。
- 1366×768：6/6 代表页面完成。
- 1024×768：6/6 代表页面完成。
- 横向溢出：0。
- 浏览器 Console error：0。
- 浏览器捕获总资产：46 张；Loading 残留 0；真实 API 验收 PASS。
- 1440 布局矩形：侧栏 256px；右侧工作区 1168.8px；内容容器扣除 24px 双侧安全边距后 1120.8px。
- 键盘焦点：统一 `focus-visible` 双像素品牌蓝轮廓。
- reduced-motion：保留系统偏好降级规则。

## 产品语义专项

- 正式导航不再出现“会员邀请”“会员入组”或正式“健管师邀请”。
- 服务关系统一展示为“客户服务邀约”“服务客户”“客户服务接入”，未包装为会员购买或付费权益。
- 平台身份流程统一展示为“用户实名认证审核”。
- 健管师正式名单展示为“健管师团队”；一期邀请路由保留为“试点健管师接入”，并明确不代表正式开放注册流程。
- 路由、API path、DTO、权限、错误合同及后端枚举未变。
- 客户服务接入详情的 `VERIFIED` 与 `PLATFORM_REVIEWING` 均为机构只读；未知实名状态 fail-closed。
- 流程条区分已完成、当前和待处理步骤，并包含“服务同意已确认”；只有 `ACCEPTED` 视为主健管师完成，只有 `PENDING_ACCEPTANCE` 可取消。
- 页面使用健管师业务名称作为主信息，移除重复版本与“入组摘要”旧术语；非法状态组合显示受控错误。

## 安全边界

- 登录页、输入框值、JWT、Cookie、Storage 和请求体未进入截图或证据。
- 完整身份信息默认隐藏；截图只出现脱敏证件。
- 健康档案只展示获批摘要、最新指标和报告元数据，不展示病史、过敏、用药、症状或报告原件。
- 页面明确显示“输入条件齐备不等于已产生评估结果”。

## Disposable 清理

- PostgreSQL 容器：0
- 8000 / 5174 监听端口：0
- FastAPI / Vite / 验收控制器进程：0（Vite 子进程按精确 PID 完成最终清理）
- 一次性凭据文件：0
- 本轮临时验收目录：0
- 浏览器验收标签页：已关闭；临时视口：已重置
- Docker 容器、Network、Volume：0

## 最终独立复审

- 复审范围：3 个前端源文件、2 个证据文档、26 个重制/保留截图资产，以及 PR #64、PR #65 的普通 develop 合并差异。
- 前端源文件规范化 Git blob：`f16e794c921e09e37fba225be15b8e244a55cc18`、`55d4a381f8f077de2bde1c3e9248e4de75fba016`、`11dddd754af83340f1ca7996fb0f12fc1fa8f26e`；与格式审计前 WIP 一致，没有夹带整文件格式化。
- Backend / Migration / SQL / package-lock 人为差异：0 / 0 / 0 / 0。
- 真实页面：横向溢出 0、Loading 残留 0、Console error 0；截图和证据未发现真实 PII、PHI、Token、Credential 或内部 URL。
- 结论：Critical / High / Important / Minor = 0 / 0 / 0 / 0。

## 组织树真实演示数据纠偏（2026-08-24）

- 数据环境：Fresh Disposable PostgreSQL、当前正式 Migration、正式 FastAPI，仅绑定 loopback。
- 根节点：受控建立 `康邻智汇总部`，`org_type=headquarter`，`parent_id=NULL`。
- 其余 10 个节点：全部通过正式 `POST /api/v1/platform/organizations` 创建，没有前端 Mock、硬编码 children 或直接改写接口响应。
- 四级链：总部 1、省 2、市 3、区/县 5，共 11 个节点；区与县统一使用 `org_type=county` 并处于相同第四级。
- `GET /api/v1/platform/organizations/tree?include_archived=false`：平台管理员 200；缺少 Authorization 401；非平台角色 403。
- 页面交互：根节点收起隐藏全部后代；浙江省收起只隐藏本省后代；节点选中高亮；总部、省、市、区/县缩进依次递增；区、县视觉深度一致。
- 三视口：1440×900、1366×768、1024×768 均无 Loading、横向溢出或 Console error；生产组织树组件及右侧详情未修改。
- 禁止数据名单逐项扫描：0 命中；不存在非总部根节点、孤儿节点或历史机构组织。
- 截图安全：未出现真实手机号、身份证号、Token、Credential、Database URL 或内部错误。
