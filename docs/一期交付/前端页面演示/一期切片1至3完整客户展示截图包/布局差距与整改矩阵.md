# 一期切片1—3桌面端布局差距与整改矩阵

| 类型 | 原问题 | 本轮整改 | 验收结果 |
|---|---|---|---|
| Shell | 1440 下侧栏与主内容共同造成右侧空白 | 大屏侧栏固定 256px；1024 折叠为 64px；主区自适应 | PASS |
| 内容容器 | 多页面统一窄宽，列表和看板没有利用工作区 | 目标页面上限统一收敛到 1280px，列表允许接近 full width | PASS |
| 工作台 | 指标、任务与判断区纵向堆叠 | 指标区保留全宽，任务/判断形成业务双栏 | PASS |
| 表单记录页 | 创建表单与历史记录上下堆叠 | 机构邀请、健管师邀请、会员邀请改为表单＋记录双栏 | PASS |
| 列表详情页 | 队列占左侧，详情缺少稳定任务区 | 健管师审核、状态页改为队列＋详情；选中后同步展开 | PASS |
| 详情页 | 审核操作与对象信息没有层级 | 对象摘要置顶，主体信息＋状态/决定侧栏 | PASS |
| 会员审核 | 列表与详情存在窄容器 | 列表全宽；详情采用资料主体＋决定侧栏 | PASS |
| 会员邀请 | 1440 双栏下记录表产生内部横向滚动 | 表单宽度缩至 340px，记录表最小宽度降至 680px | PASS |
| 响应式 | 1024 侧栏占用导致内容压缩 | 1024 使用图标侧栏，双栏按断点安全堆叠 | PASS |
| 可访问性 | 焦点与动态效果需复核 | 键盘焦点可达；保留 reduced-motion 规则；状态颜色配合文字 | PASS |
| 展示文案 | `PREPARING`、`Evidence`、`super_admin` 暴露内部英文 | 仅在显示层映射为“服务准备中”“就绪证据”“平台管理员” | PASS |
| 演示数据 | 旧合成门店、区域和测试对象名称生硬或含英文 | Disposable 控制器生成自然中文脱敏演示名称 | PASS |

## 未改变的语义

- API method/path/header/body 未改变。
- 角色与 Scope 未改变。
- 409、503、幂等与 PII 清理语义未改变。
- 未引入 Slice 4、未实现 APP UI。
- 未修改 Backend、Migration、SQL 或数据库权限。

## 待技术负责人确认的非阻断项

1. 受控入驻审核全量截图保留“左侧待处理项＋右侧选择提示”的首屏状态；它未进入 CEO 12 张候选。
2. 仓库全局格式检查仍受既有 CRLF 基线影响；本轮 16 个变更文件的 CRLF-safe Biome 检查已通过，未批量格式化历史文件。
3. 既有 4 个 npm high 依赖告警未在本任务中升级或修改锁文件。
