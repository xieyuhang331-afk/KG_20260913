# 一期切片1—3完整客户展示测试与真实接口证据

## 固定基线

- 分支：`feature/phase1-complete-customer-demo-screenshot-package`
- 最新 `develop` 合并基线：`b3e29c52f9c3d590ae27950a0852c5a10b0d6897`
- 前端内容变更：16 个文件（15 个生产文件＋1 个既有测试文件）。
- Backend / Migration / SQL 人为差异：`0 / 0 / 0`
- package.json / package-lock.json 差异：`0 / 0`

## 真实接口与数据

- Fresh Disposable PostgreSQL。
- 正式 Migration 至 `20260823_0028`。
- 正式 Runtime 角色与真实 FastAPI。
- 真实 Vite 页面和 JWT 登录流程。
- 数据全部为合成门店、组织、健管师、会员和材料摘要。
- 未使用 Mock 作为截图闭环证据；未连接共享测试、UAT、Staging 或 Production。
- Disposable 控制器只在仓库外用于数据准备与生命周期管理，未进入 Git。

## 前端质量门禁

- `npm ci`：PASS；锁文件可复现。
- Vitest：`9 test files / 116 tests / 0 failed / 0 skipped`。
- TypeScript + Production Build：PASS。
- Lint：PASS；46 个批准门禁文件。
- 本轮 16 个变更文件 CRLF-safe Format Check：PASS。
- 全局 Format Check：既有 CRLF 基线仍会提示；未批量改写历史文件。
- PII/PHI/Credential 扫描：PASS，0 命中。
- `git diff --check`：PASS。
- 既有 npm audit：4 high，未执行 audit fix，未改锁文件。

## 浏览器与视觉验收

- 21/21 页面输出 1440×900。
- 6 个代表页面输出 1366×768 和 1024×768。
- 页面级横向溢出：0。
- 表格/表单/操作控件截断：0。
- Loading、失败、权限拒绝或服务未就绪截图进入交付包：0。
- 浏览器未处理 Console error：0。
- 键盘焦点可达；reduced-motion CSS 规则存在。
- 完整 PII、JWT、Cookie、Credential、数据库连接或私有材料 URL 截图：0。
- 真实 HTTP 核心读取：平台审核、会员实名、组织树、机构健管师、服务就绪、会员邀请与入组均为 200。
- 真实 HTTP 安全错误：401、403、404、409、422 均按合同返回；关闭 Disposable PostgreSQL 后真实依赖故障返回 503，全部未被前端包装成成功。

## Disposable 清理

- PostgreSQL container：0。
- Disposable network / volume：0。
- 8000 / 5174 / 5175 / 5176 / 5177 / 17680 监听：0。
- 运行状态文件、一次性 Credential 文件、停止标志：0。
- 临时私有材料目录：0。
- 浏览器视口已恢复默认。
