# 一期切片7前端验收证据

## 验收基线

- 分支：`feature/phase1-slice7-frontend-acceptance-productization`
- 后端基线：`e52167f3727dc6ec97082bbe16b319d50f542272`
- Migration Head：`20260827_0032`
- 环境：Fresh Disposable PostgreSQL、正式 Migration、真实 FastAPI、真实 Vite，仅绑定 loopback。17/17 正式 HTTP 验收沿用已通过的隔离后端证据；本轮截图复验只执行只读页面请求，因此未启动 RabbitMQ。

## 页面与接口

| 端 | 页面 | 正式能力 |
|---|---|---|
| 机构端 | 服务履约列表/详情 | cursor 列表、D0—D28 里程碑、风险与生命周期、暂停/恢复/终止 |
| 机构端 | 转机构列表/详情 | 受控范围、状态、接收/拒绝/源机构关闭边界、Continuation Handoff 只读信息 |
| 平台端 | 履约监督列表/详情 | 全局监督、风险与生命周期、安全终止 |
| 平台端 | 转机构监督列表/详情 | 全局监督、coordinate close 受控边界 |
| 平台端 | 个人数据导出监督列表/详情 | 导出任务状态与审计监督，不提供下载入口 |

## 真实 HTTP

- Slice 7 正式 HTTP 验收：17/17 PASS。
- 401、403、404、409、422、503：按正式错误合同处理。
- 409：重新获取服务端权威状态，不自动重放 mutation。
- `COMMIT_OUTCOME_UNKNOWN`：先查询权威状态，不更换 Idempotency-Key 盲目重提。
- 写操作携带最新 `version`；cursor 保持服务端 opaque string，不解析、不显示、不推导总页数。
- org_operator 对机构端写操作保持拒绝；平台端无个人导出文件下载入口。

## 浏览器与安全

- 视口：1440×900、1280×800、1024×768。
- 横向溢出：0。
- Console Error：0。
- 未发现原始 SQL、堆栈、数据库路径、Credential、JWT、PII 或 PHI 页面泄漏。
- DataScope 使用正式枚举并映射为中文业务名称。
- org_operator 页头身份显示已纠正为“机构运营人员”，权限语义未改变。
- 23 张截图均由当前真实 API 页面重新写入磁盘，并完成逐文件 SHA-256 固化。

## 最终前端质量门禁

- Slice 7 定向测试：3 个文件，20/20 PASS。
- 完整 Vitest：16 个文件，193/193 PASS，failure/error/skipped = 0/0/0。
- TypeScript、Production Build、完整 Lint、Slice 7 Lint、changed-file Format：PASS。
- PII/PHI/Credential 扫描：0 命中。
- `git diff --check`：PASS；仅存在 Windows LF/CRLF 转换提示。
- Backend、Migration、SQL、CI Workflow 与 package-lock 非预期差异：0。

## Disposable 清理

- PostgreSQL / RabbitMQ 容器：0。
- 任务专属 Network / Volume：0。
- 8000 / 5174 监听：0。
- 临时控制器、浏览器会话、日志、环境文件和一次性凭据残留：0。

## 完成边界

- 本证据不代表部署授权。
- 本任务未修改 Backend、Migration、SQL、CI Workflow 或 package-lock。
- 本任务未启动 Family APP、Therapist APP 或后续业务模块。
