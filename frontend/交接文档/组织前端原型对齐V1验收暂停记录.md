# Organization Frontend Prototype Alignment V1 最终验收证据

## 冻结点

- 分支：`feature/organization-frontend-prototype-alignment-v1`
- 基线：`origin/develop@add1b5e58e4a2146de5bba9c2b858e6b7c017a10`
- Organization Foundation Frontend V1 ancestry：已确认
- 本记录固化时尚未 Commit、Push、创建 PR 或触发远端 CI；这些步骤仅在最终独立复审 0/0/0 后执行。

## 已完成证据（2026-08-11）

- 平台 Organization 页面已使用 disposable PostgreSQL、Migration head、合成四级组织与 tenant 数据连接真实 API。
- 平台端已验证四级树、游标上一批/下一批、archived 节点、legacy 只读与 1440/1366/1024 响应式；1024 横向溢出为 0。
- 平台组织页响应式断点收敛到 1180：1440 与 1366 使用左侧固定组织树、右侧详情/门店列表双栏；1024 安全回落为单栏，三宽度横向溢出均为 0。
- `org_admin` 浏览器登录已成功进入机构端；确认不是前端 Auth、Role Guard 或后端合同故障，上一轮 `LOGIN_FAILED` 属于等待方式误判。
- 机构端真实 API 已验证 assigned、unassigned、legacy、archived 安全错误与恢复 assigned；平台树访问跳转 `/403`。
- 机构端 1440/1366/1024 均无横向溢出；1440 脱敏截图已固化，1366/1024 状态核验通过但截图未固化。
- “总部、省、市、区/县四级治理树”、`county` 显示“区/县”、城市节点“新增区/县”已修正；请求仍固定提交 `org_type=county`。
- `npm ci`：PASS；既有 4 个 high 漏洞未升级，继续留在独立 Dependency Governance。
- Organization 定向测试：39/39 PASS；Auth 定向测试：2/2 PASS；完整前端测试：72/72 PASS。
- TypeScript / Production Build、lint、format check、PII scan：PASS。
- SA 真实 API：创建区/县、改名、管理员解绑/恢复绑定、停用/激活、同父原子排序、游标上一批/下一批、legacy/archived 只读全部 PASS。
- PA 真实 API：仅本省 Scope、越 Scope 安全 404、archived 不可读、治理入口不可见全部 PASS。
- CA 真实 API：仅本市 Scope、区/县节点可见、越 Scope 安全 404、治理入口不可见全部 PASS。
- 409 冲突刷新且不自动重放由 Organization 定向测试覆盖；浏览器并发尝试在确认框控制通道中断前未形成第二次提交。
- 平台干净业务页 console error=0；键盘焦点可见、交互元素 Tab 可达、状态颜色与文字并存、reduced-motion 规则有效。

## Credential 安全与环境清理

- 全新 disposable run 的 Backend 随机账号登录预检 3/3 成功；Human 通过仅限 loopback 的一次性本机交付页手工登录 SA、PA、CA。
- 未脚本填充 Credential、未注入 Token、未读取 Cookie 或 Storage 值；登录页未截图、未生成 DOM/HTML/请求体 Evidence。
- Credential、JWT、数据库密码只存在于临时进程、环境变量与 disposable database；未进入聊天、Git、Markdown、JUnit、截图或日志。
- disposable database、临时 roles、Backend、三个 Frontend、controller、containers、network 与临时 runtime 已销毁。
- 8000、17648、17649、17650、17651 监听残留：0；container/network/runtime 残留：0。
- 浏览器验收标签页与 viewport override 已清理；随机 Credential 与 JWT 随进程和密钥销毁而失效。

## 最终本地 Gate

- `npm ci`：PASS（既有 4 个 high 不在本模块处理）。
- Build / TypeScript：PASS。
- Tests：72/72 PASS。
- lint / format check：PASS。
- PII scan：0 命中。
- 独立复审、Commit、Push、PR、CI：等待下一检查点执行。
